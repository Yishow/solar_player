## 0. Inventory and verification matrix

- [ ] 0.1 完成 “Maintain a per-page prototype-to-runtime mapping for all 14 pages”，逐一列出 prototype `01-14`、對應 React route、目前頁面檔、主要共享元件、主要 runtime data source、reference-only content、fallback-only content；驗證方式為內容 review，確認 `01-14` 沒有任何一頁漏列或只寫 route 名稱沒有資料來源。
- [ ] 0.2 完成 “Record per-phase verification targets and evidence expectations”，把每個後續 phase 對應到要跑的 web build、web tests、server tests、manual routes、prototype screenshot 對照來源；驗證方式為內容 review，確認每一個 phase 都至少有一條命令驗證與一條人工視覺驗證。
- [ ] 0.3 完成 “Classify each mapped region as runtime-bound, reference-only, or fallback-only”，讓每頁 mapping 都明確標示 prototype 區塊屬性與 fallback 策略；驗證方式為內容 review，確認沒有任何 prototype sample value 被寫成新的 runtime contract。
- [ ] 0.4 完成 “Enforce hard phase gates for the visual rollout” 並對應 ### Freeze the execution order with hard phase gates，明確記錄「未完成前一 phase exit criteria 不得開始下一 phase」；驗證方式為內容 review，確認 tasks 中每個 phase 都有前置條件或依賴敘述。
- [ ] 0.5 完成 “Split route migration by functional risk and coupling”，把播放頁與管理頁按共享 primitive、互動風險、runtime-sensitive behavior 拆成 `Overview + Solar`、`Factory Circuit`、`Images + Sustainability`、`Playback Settings + Image Management`、`MQTT Settings`、`Circuit Settings`、`Monitoring and Maintenance` 等批次；驗證方式為內容 review，確認沒有任何一批同時混入高風險互動頁與不相干的低風險展示頁。

## 1. Shared shell foundation

- [ ] 1.1 完成 ### Finish shell primitives before any route-specific migration 的 token 與 shell extraction，從 `docs/reference/kuozui-green-fhd-html-prototype/styles/tokens.css`、`docs/reference/kuozui-green-fhd-html-prototype/styles/shell.css`、`docs/reference/kuozui-green-fhd-html-prototype/styles/components.css` 盤出 header、footer、page number、title block、section wrapper、status pill、action cluster、media slot、density variants 所需規則；驗證方式為內容 review，確認 shell primitives 清單與 prototype 檔案來源一一對應。
- [ ] 1.2 完成 “Provide a shared FHD kiosk shell before route-specific migration”，更新 `apps/web/src/layouts/LayoutShell.tsx`、`components/AppHeader.tsx`、`components/AppFooterNav.tsx`、`pages/shared/PageScaffold.tsx`、`components/PageContainer.tsx`、`components/PageNumberPill.tsx`，讓 `/overview` 與 `/settings/playback` 共用同一套 FHD shell contract；驗證方式為執行 `pnpm --filter @solar-display/web build`，並人工檢查兩條 route 的 header/footer/page number 結構一致。
- [ ] 1.3 完成 “Expose reusable shell primitives and density variants”，補齊 `title block`、`section wrapper`、`status pill`、`action cluster`、`media slot` 與 playback/admin/device-detail density 規則；驗證方式為確認至少一條 playback route 與一條 management route 已引用共享 primitives，並再次執行 `pnpm --filter @solar-display/web build`。
- [ ] 1.4 完成 “Verify the shell contract on at least one playback route and one management route”，對 `/overview` 與 `/settings/playback` 保存 shell screenshot、build 命令輸出與人工對照摘要；驗證方式為內容 review，確認 evidence bundle 同時覆蓋一條 playback route 與一條 management route。
- [ ] 1.5 完成 “Save an evidence bundle at every phase boundary” 的 Phase 1 收尾，保存 route 清單、命令輸出、人工視覺摘要與仍未完成的 page-specific gap note；驗證方式為內容 review，確認 evidence bundle 包含 route、命令、人工視覺摘要三類資訊。

## 2. Playback batch A: Overview and Solar

- [ ] 2.1 完成 “Migrate playback pages in declared batches instead of one five-page rewrite” 並對應 ### Split playback pages by coupling instead of migrating all five together，先只處理 `/overview` 與 `/solar` 這一批，不把 `/factory-circuit`、`/images`、`/sustainability` 混入同一次實作；驗證方式為內容 review，確認 Batch A 僅覆蓋 `01` 與 `02`。
- [ ] 2.2 完成 `playback-page-visual-alignment` 的 `/overview` 遷移，讓 `apps/web/src/pages/Overview/index.tsx` 對齊 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/01-overview.html` 的 hero、KPI、summary 與 shell rhythm，同時保留 `useLiveMetrics` 與 fallback metrics 行為；驗證方式為執行 `pnpm --filter @solar-display/web build`，並人工對照 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/01-overview.html` 檢查區塊與資訊層級。
- [ ] 2.3 完成 `playback-page-visual-alignment` 的 `/solar` 遷移，讓 `apps/web/src/pages/Solar/index.tsx` 對齊 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/02-solar.html` 的流程圖、即時 KPI 與 section hierarchy，同時保留既有 route 與現有資料對接策略；驗證方式為執行 `pnpm --filter @solar-display/web build`，並人工對照 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/02-solar.html` 檢查 flow/metric composition。
- [ ] 2.4 完成 ### Bind runtime data through page-local adapters 與 ### Preserve runtime contracts while allowing bootstrap-only prototype alignment 的 Playback Batch A adapter 整理，把 `/overview` 與 `/solar` 的 prototype 區塊所需欄位集中在 page-local adapter 或 view-model 層，不把 layout 與 mapping 雜揉在 JSX，也不把 prototype sample value 升格成 runtime contract；驗證方式為 code review，確認頁面 JSX 沒有散落的 reference-only 判斷與多處重複格式轉換。
- [ ] 2.5 完成 “Preserve playback runtime behavior across all playback batches”，確認 `/overview` 與 `/solar` 在改版後仍維持 route rotation、offline redirect、live metric fallback；驗證方式為執行 `pnpm --filter @solar-display/web test -- src/hooks/playbackRouteNavigation.test.ts src/hooks/playbackRouteSync.test.ts src/layouts/offlineRouting.test.ts`，並人工檢查斷線時 route 行為未改變。

## 3. Playback batch B: Factory Circuit

- [ ] 3.1 延續 “Migrate playback pages in declared batches instead of one five-page rewrite”，獨立處理 `/factory-circuit` 這一批，不與 `/images` 或 `/sustainability` 混做；驗證方式為內容 review，確認 Batch B 只覆蓋 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/03-factory-circuit.html`。
- [ ] 3.2 完成 `playback-page-visual-alignment` 的 `/factory-circuit` 遷移，讓 `apps/web/src/pages/FactoryCircuit/index.tsx` 對齊 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/03-factory-circuit.html` 的 flow diagram、circuit cards、status density 與 summary rhythm；驗證方式為執行 `pnpm --filter @solar-display/web build`，並人工對照 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/03-factory-circuit.html` 檢查流程圖與 circuit card 結構。
- [ ] 3.3 完成 ### Bind runtime data through page-local adapters 與 ### Preserve runtime contracts while allowing bootstrap-only prototype alignment 的 `/factory-circuit` adapter 與閾值映射，明確定義 circuit API 到 UI status label、status color、load percentage、fallback empty state 的轉換；驗證方式為 code review，確認 `warningMin`、`attentionMin` 與空資料狀態都在集中映射層處理。
- [ ] 3.4 完成 “Preserve playback runtime behavior across all playback batches”，確認 `/factory-circuit` 改版後仍維持播放頁 route contract、離線 fallback 與空 circuits 狀態不破版；驗證方式為執行 `pnpm --filter @solar-display/web build` 與 `pnpm --filter @solar-display/web test -- src/layouts/offlineRouting.test.ts`，並人工檢查無資料時版面完整。

## 4. Playback batch C: Images and Sustainability

- [ ] 4.1 延續 “Migrate playback pages in declared batches instead of one five-page rewrite”，獨立處理 `/images` 與 `/sustainability` 這一批，不回頭重混前兩批播放頁；驗證方式為內容 review，確認 Batch C 僅覆蓋 `04` 與 `05`。
- [ ] 4.2 完成 `playback-page-visual-alignment` 的 `/images` 遷移，讓 `apps/web/src/pages/Images/index.tsx` 對齊 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/04-images.html` 的主媒體框、縮圖帶、資訊側欄與切換控制，同時保留目前的圖片切換與 placeholder 行為；驗證方式為執行 `pnpm --filter @solar-display/web build`，並人工對照 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/04-images.html` 檢查 media slot 與 thumbnail hierarchy。
- [ ] 4.3 完成 `playback-page-visual-alignment` 的 `/sustainability` 遷移，讓 `apps/web/src/pages/Sustainability/index.tsx` 對齊 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/05-sustainability.html` 的 hero、big-number blocks、ESG contribution 區塊與敘事節奏，同時保留 mock/fallback summary contract；驗證方式為執行 `pnpm --filter @solar-display/web build`，並人工對照 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/05-sustainability.html` 檢查 big-number 與 ESG 區塊。
- [ ] 4.4 完成 “Degrade gracefully when playback data or media is unavailable”，確認 `/images` 無 runtime asset 與 `/sustainability` 無 live summary 時仍有 placeholder/fallback presentation；驗證方式為人工檢查移除或停用資料來源時頁面不空白不破版，並更新 mapping artifact 的 fallback-only 欄位。
- [ ] 4.5 完成 “Save an evidence bundle at every phase boundary” 的 Playback phase evidence bundle，保存 `01-05` 對照 screenshot、驗證命令結果與 playback-specific gap note；驗證方式為內容 review，確認五條播放 route 都有對應證據與未完成註記。

## 5. Management batch A: Playback Settings and Image Management

- [ ] 5.1 完成 “Migrate management routes in risk-based batches” 的第一批，先只處理 `/settings/playback` 與 `/settings/images`，不把 `/settings/mqtt`、`/settings/circuits` 或監控頁混入；驗證方式為內容 review，確認 Batch A 僅覆蓋 `07` 與 `08`。
- [ ] 5.2 完成 `management-page-visual-alignment` 的 `/settings/playback` 遷移，讓 `apps/web/src/pages/PlaybackSettings/index.tsx` 對齊 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/07-playback-settings.html` 的設定分區、排序區、schedule controls 與 action hierarchy，同時保留 `getPlaybackSettings`、`getPlaybackPages`、`updatePlaybackSettings`、`updatePlaybackPages` 的既有流程；驗證方式為執行 `pnpm --filter @solar-display/web build` 與 `pnpm --filter @solar-display/server test -- src/routes/playback.test.ts`，並人工 smoke test 排序、啟用開關、儲存。
- [ ] 5.3 完成 `management-page-visual-alignment` 的 `/settings/images` 遷移，讓 `apps/web/src/pages/ImageManagement/index.tsx` 對齊 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/08-image-management.html` 的資產清單、狀態標記、快速操作區與資料密度，同時保留當前 image library 的資料與操作定位；驗證方式為執行 `pnpm --filter @solar-display/web build` 與 `pnpm --filter @solar-display/server test -- src/routes/images.test.ts`，並人工檢查長標題與狀態 badge 的可讀性。
- [ ] 5.4 完成 “Preserve operational contracts for interactive management pages” 的 Batch A 驗證，確認 playback settings 的保存/重排與 image management 的操作定位仍維持現有契約；驗證方式為人工 smoke test 與對應 server tests 成功通過。
- [ ] 5.5 完成 ### Split management pages by operational risk, not by route count 的 Batch A exit review，明確記錄這一批只覆蓋低風險設定頁，不含 MQTT 與 circuits；驗證方式為內容 review，確認 evidence bundle 與 mapping artifact 都沒有把 `/settings/mqtt` 或 `/settings/circuits` 混入 Batch A。

## 6. Management batch B: MQTT Settings

- [ ] 6.1 完成 ### Isolate MQTT settings as a dedicated high-risk phase 的 `/settings/mqtt` visual segmentation，將 `apps/web/src/pages/MqttSettings/index.tsx` 對齊 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/09-mqtt-settings.html` 的 broker config、topic mapping、connection status、action area 與 dense table hierarchy；驗證方式為執行 `pnpm --filter @solar-display/web build`，並人工對照 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/09-mqtt-settings.html` 檢查區塊分層與 FHD 可讀性。
- [ ] 6.2 完成 “Preserve operational contracts for interactive management pages” 的 MQTT interaction regression，確認 load settings、load topics、save settings、save topics、test connection、錯誤訊息與 loading states 在新 layout 下仍保留；驗證方式為執行 `pnpm --filter @solar-display/server test -- src/routes/settings-mqtt.test.ts src/routes/settings-mqtt-save-regression.test.ts`，並人工 smoke test save/test/error flow。
- [ ] 6.3 完成 ### Bind runtime data through page-local adapters 與 ### Preserve runtime contracts while allowing bootstrap-only prototype alignment 的 MQTT mapping 整理，明確區分 broker status、topic row runtime fields、reference-only copy、fallback message 與 disabled state；驗證方式為 code review，確認狀態顯示與 topic row mapping 沒有散落在多個不一致的 JSX 分支中。
- [ ] 6.4 完成 “Save an evidence bundle at every phase boundary” 的 MQTT phase evidence bundle，保存成功與失敗兩種操作截圖、server test 命令輸出與未覆蓋 edge cases；驗證方式為內容 review，確認 evidence bundle 至少包含 save/test success 與 error feedback 各一組。

## 7. Management batch C: Circuit Settings

- [x] 7.1 延續 “Migrate management routes in risk-based batches”，獨立處理 `/settings/circuits`，不與 MQTT 或監控頁混為同一 phase；驗證方式為內容 review，確認 Batch C 僅覆蓋 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/10-circuit-settings.html`。
- [x] 7.2 完成 `management-page-visual-alignment` 的 `/settings/circuits` 遷移，讓 `apps/web/src/pages/CircuitSettings/index.tsx` 對齊 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/10-circuit-settings.html` 的表單分區、迴路列表、CRUD action hierarchy 與 status messaging；驗證方式為執行 `pnpm --filter @solar-display/web build`，並人工對照 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/10-circuit-settings.html` 檢查 form/list/action composition。
- [x] 7.3 完成 “Preserve operational contracts for interactive management pages” 的 Circuit CRUD regression，確認 create、update、delete、load failure 與 message/error states 都在新 layout 下保留；驗證方式為執行 `pnpm --filter @solar-display/server test -- src/routes/circuits.test.ts`，並人工 smoke test CRUD 流程與錯誤顯示。

## 8. Monitoring and maintenance batch

- [x] 8.1 延續 “Migrate management routes in risk-based batches”，把 `/trends`、`/history`、`/offline`、`/slideshow-preview`、`/device-status` 當成 monitoring and maintenance batch，與前面 settings 批次分開；驗證方式為內容 review，確認 Batch D 僅覆蓋 `06`、`11`、`12`、`13`、`14`。
- [x] 8.2 完成 `management-page-visual-alignment` 的 `/trends` 遷移，讓 `apps/web/src/pages/EnergyTrend/index.tsx` 對齊 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/06-energy-trend-summary.html` 的 chart region、range controls 與 summary hierarchy，同時不新增新的歷史資料 contract；驗證方式為執行 `pnpm --filter @solar-display/web build`，並人工對照 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/06-energy-trend-summary.html` 檢查 chart/summary 節奏。
- [x] 8.3 完成 `management-page-visual-alignment` 的 `/history` 遷移，讓 `apps/web/src/pages/EnergyHistory/index.tsx` 對齊 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/11-energy-data-history.html` 的高密度表格、累積摘要與峰值資訊分區；驗證方式為執行 `pnpm --filter @solar-display/web build`，並人工檢查長欄位、數字與時間欄的 FHD 可讀性。
- [x] 8.4 完成 `management-page-visual-alignment` 的 `/offline` 遷移，讓 `apps/web/src/pages/OfflineError/index.tsx` 對齊 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/12-offline-error-display.html` 的離線告警版面，同時保留 retry countdown、returnTo 導航與 reconnect 行為；驗證方式為執行 `pnpm --filter @solar-display/web test -- src/layouts/offlineRouting.test.ts`，並人工檢查離線與恢復時的 route 行為。
- [x] 8.5 完成 `management-page-visual-alignment` 的 `/slideshow-preview` 遷移，讓 `apps/web/src/pages/SlideshowPreview/index.tsx` 對齊 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/13-slideshow-preview.html` 的 live preview、progress、playback controls 與 page queue hierarchy，同時保留 `usePageRotation` 的既有控制流程；驗證方式為執行 `pnpm --filter @solar-display/web test -- src/hooks/playbackRouteNavigation.test.ts src/hooks/playbackRouteSync.test.ts`，並人工檢查 prev/next/play control。
- [x] 8.6 完成 `management-page-visual-alignment` 的 `/device-status` 遷移，讓 `apps/web/src/pages/DeviceStatus/index.tsx` 對齊 `docs/reference/kuozui-green-fhd-html-prototype/html-pages/14-device-status-details.html` 的 system info、resource cards、maintenance actions 與 error/fallback states；驗證方式為執行 `pnpm --filter @solar-display/web build`，並人工檢查 reboot/clear-cache 行為在失敗時仍有合理回饋。
- [x] 8.7 完成 “Keep dense monitoring and maintenance content readable at FHD scale”，確認 `/trends`、`/history`、`/offline`、`/slideshow-preview`、`/device-status` 的圖表、表格、狀態文字、維護操作在 FHD 下可讀；驗證方式為人工檢查長資料與高密度區塊不擠壓關鍵控制項。
- [x] 8.8 完成 “Save an evidence bundle at every phase boundary” 的 Monitoring batch evidence bundle，保存 `06`、`11`、`12`、`13`、`14` 的 screenshot、命令輸出、長資料可讀性摘要與 runtime-sensitive 行為檢查；驗證方式為內容 review，確認五條 route 都有證據與未完成註記。

## 9. Cross-route verification and handoff

- [ ] 9.1 完成 `prototype-to-runtime-page-mapping` 的最終 coverage review，確認 `01-14` 每頁都已有 prototype source、React route、主要 shared primitives、主要 data sources、reference-only content、fallback-only content、驗證命令與 screenshot evidence；驗證方式為內容 review，逐頁核對不得有空欄、不得只寫「同前頁」。
- [ ] 9.2 完成 ### Require evidence bundles at every phase boundary 的總收尾，執行 `pnpm --filter @solar-display/web build`、`pnpm --filter @solar-display/web test`、`pnpm --filter @solar-display/server test`、至少一輪 14 route walkthrough，並記錄 shell consistency、page number、footer navigation、offline behavior、settings smoke 結果；驗證方式為保存命令輸出與人工 walkthrough 摘要。
- [ ] 9.3 完成 Spectra artifact 收尾，執行 `spectra analyze align-solar-display-with-fhd-prototype --json` 與 `spectra validate align-solar-display-with-fhd-prototype`，確認 change artifacts 與 phase-gated rollout contract 一致；驗證方式為兩個命令都成功通過。
