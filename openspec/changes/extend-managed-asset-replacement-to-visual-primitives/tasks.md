## Tasks

- [ ] Implement `Managed Visual Source Mode` for icon and ornament source schemas while preserving existing direct, glyph, registry, and ornament fallback modes.
- [ ] Implement `Display editor can replace existing visual primitives with managed assets` by applying `Replacement Coverage` for hero/background media, Images main stage, card icons, flow/node icons, placeholder icons, leaf ornaments, and shared shell ornament images.
- [ ] Implement `Managed visual replacements fall back safely` with `Runtime Resolution` so managed assets render first and unresolved references fall back predictably.
- [ ] Implement `Managed visual replacements participate in asset governance` with `Asset Governance` so managed icon and ornament references appear in asset health, usage panels, and delete guards.
- [ ] Wire editor fields and gallery-backed pickers for representative visual primitive replacement surfaces.
- [ ] Add tests for managed card icon replacement, leaf ornament replacement, runtime fallback, and asset governance references.
- [ ] Run affected shared, server, and web tests.
