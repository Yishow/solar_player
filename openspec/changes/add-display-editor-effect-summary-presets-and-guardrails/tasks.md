## Tasks

- [x] Implement `Summarize the active effect stack in a read-only companion surface` for Source Connection or the adjacent summary area.
- [x] Implement `Offer editable presets for common composable effect setups` by `Offer editable presets as accelerators, not locked templates` for common effect combinations.
- [x] Implement `Provide guardrails for extreme ranges and conflicting stacks` by `Guard against unreadable or self-canceling stacks` with range feedback and stacking conflict hints.
- [x] Add tests for summary readability, preset-generated editable layers, and guardrail feedback.
- [x] Run affected web tests for DisplayPagesEditor effect summaries and presets.
