## Tasks

- [ ] Implement `Editor asset pickers provide gallery-backed visual selection` by applying `Picker Card Model` with thumbnail asset cards and selected preview.
- [ ] Implement `Eligibility Filtering` for page object and shell decoration contexts.
- [ ] Implement `display-editor-gallery-backed-asset-picker` search or category filtering inside picker scope.
- [ ] Implement `Editor asset pickers preserve editing context when opening the asset workspace` by applying `Asset Workspace Link` to page and shell picker entry points.
- [ ] Preserve existing managed asset reference payloads and fallback source updates.
- [ ] Add tests for card rendering, selected state, eligibility filtering, and asset workspace link behavior.
- [ ] Run affected web tests for DisplayPagesEditor, ShellDecorationEditor, and AssetLibrary.
