## Tasks

- [x] Implement `Render normalized media effect layers into bounded composable presentation` by `Render composable zones from normalized layer data` in the shared media presentation builder.
- [x] Implement `Preserve same-zone stacking order for composable effects` by `Separate localized overlay rendering from whole-media filters` so full-frame and zone-bounded effects can coexist.
- [x] Implement `Keep composable effect overlays bounded to the owning media layer` by `Keep overlays clipped to the media owning layer` in runtime and preview rendering.
- [x] Add rendering tests for top/bottom/left/right/dual-edge/all-edge zones, same-zone stacking, and bounded overlays.
- [x] Run affected web tests for shared media rendering and live preview surfaces.
