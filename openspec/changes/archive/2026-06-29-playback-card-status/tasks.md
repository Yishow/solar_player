## 1. 共用資料模型

- [x] 1.1 依設計決策「設置中與隱藏採用兩個正交欄位而非單一三值列舉」，在 `packages/shared/src/displayPageCardRail.ts` 的卡型別新增可選 `status?: "normal" | "configuring"` 欄位，並讓建構/型別守衛把缺欄位視為 `normal`，落實 requirement「Playback data cards support a per-card configuring status」的資料模型基礎。完成後：card rail 卡可攜帶 status、缺省即正常。驗證：`pnpm run build` 通過，新增 shared 測試斷言「省略 status 的卡解析為 normal、設 `configuring` 時保留」。
- [x] 1.2 [P] 依設計決策「editor 沿用既有欄位型別，不新增 schema 種類」，在 `apps/web/src/pages/DisplayPagesEditor/cardRailTemplateFields.ts` 的 `buildCardRailCardFields` 暴露顯示開關（`DisplayEditorToggleFieldSchema` 綁 `visible` path）與狀態選單（`DisplayEditorSelectFieldSchema` 選項 `正常`/`設置中` 綁 `status` path），落實 requirement「Editor exposes a configuring status control that persists through draft and live publishing」與「Editor exposes a visibility toggle that persists through draft and live publishing」在 card rail 卡的覆蓋。完成後：card rail 卡 inspector 出現顯示與狀態兩控制。驗證：新增/更新 editor schema 測試斷言兩欄位存在且 path 正確。

## 2. 各頁卡片狀態（editor 控制 + runtime 呈現）

- [x] 2.1 [P] Overview：在 `apps/web/src/pages/Overview/displayPageConfig.ts` 為 `kpiCards.{power,todayGeneration,totalGeneration,todayCo2,totalCo2}` 新增 `status` 與 editor 狀態選單（保留既有 `visible` toggle），並在 `apps/web/src/pages/Overview/index.tsx`/`viewModel.ts` 落實 requirement「Configuring status is display-only and does not alter data」與「Hidden takes precedence over configuring」：`status: "configuring"` 在數值位置輸出「設置中」且不停背後 binding、`visible: false` 不渲染、隱藏優先。驗證：擴充 `Overview/configRender.test.tsx`/`viewModel.test.ts` 斷言 configuring 顯示「設置中」且 hidden 不渲染。
- [x] 2.2 [P] Solar：依設計決策「story-runtime 驅動卡片以 per-card 狀態 map 取得 config 落點」，在 `apps/web/src/pages/Solar/displayPageConfig.ts` 新增以 KPI metric key 為鍵的 per-card 狀態 map（`{ visible?, status? }`）與對應 editor region fields，並在 `apps/web/src/pages/Solar/index.tsx`/`viewModel.ts` 套用 configuring→「設置中」、hidden→不渲染。驗證：擴充 `Solar/configRender.test.ts`/`viewModel.test.ts` 涵蓋 configuring 與 hidden。
- [x] 2.3 [P] FactoryCircuit：依設計決策「story-runtime 驅動卡片以 per-card 狀態 map 取得 config 落點」，在 `apps/web/src/pages/FactoryCircuit/displayPageConfig.ts` 新增以迴路 slot key 為鍵的 per-card 狀態 map 與 editor region fields，並在 `apps/web/src/pages/FactoryCircuit/index.tsx`/`viewModel.ts` 套用 configuring→「設置中」、hidden→不渲染。驗證：擴充 `FactoryCircuit/configRender.test.ts`/`viewModel.test.ts` 涵蓋 configuring 與 hidden。
- [x] 2.4 [P] Images：在 `apps/web/src/pages/Images/displayPageConfig.ts` 為 caption 資料卡新增 `status` 與 editor 控制，並在 `apps/web/src/pages/Images/index.tsx`/`viewModel.ts` 套用 configuring→「設置中」、hidden→不渲染。驗證：擴充 `Images/configRender.test.ts`/`viewModel.test.ts` 涵蓋 configuring 與 hidden。
- [x] 2.5 [P] Sustainability：在 `apps/web/src/pages/Sustainability/displayPageConfig.ts` 為大數字卡與 `household-today`/`household-cumulative` 新增 `status` 與 editor 顯示/狀態控制，並在 `apps/web/src/pages/Sustainability/index.tsx`/`viewModel.ts`（含 card rail 渲染）套用 configuring→「設置中」、hidden→不渲染、隱藏優先。驗證：擴充 `Sustainability/configRender.test.ts`/`viewModel.test.ts` 涵蓋原生卡與 card rail 卡的 configuring 與 hidden。
- [x] 2.6 [P] FactoryCircuit KPI 卡：依設計決策「story-runtime 驅動卡片以 per-card 狀態 map 取得 config 落點」，在 `apps/web/src/pages/FactoryCircuit/displayPageConfig.ts` 為頂部 KPI 卡（flow/peak/selfConsumption/solarShare/totalPower）新增以 `FactoryCircuitKpiKey` 為鍵的 `kpiCardStates` map（`{ visible?, status? }`）與 editor region fields，並在 `apps/web/src/pages/FactoryCircuit/index.tsx` 套用 configuring→「設置中」、hidden→不渲染，落實 requirement「Configuring status is display-only and does not alter data」與「Hidden takes precedence over configuring」於 Factory KPI 卡。驗證：擴充 `FactoryCircuit/configRender.test.ts` 斷言每個 `factory-kpi-*` region 含 visible toggle 與 status select、seed 預設 normal/visible、runtime 套用 placeholder。
- [x] 2.7 [P] Sustainability statCards：在 `apps/web/src/pages/Sustainability/displayPageConfig.ts` 為 stat 卡（esg/procure/trees，含植樹等效）新增以 stat key 為鍵的 `statCardStates` map 與 editor region fields，並在 `apps/web/src/pages/Sustainability/index.tsx` 套用 configuring→「設置中」、hidden→不渲染，落實 requirement「Configuring status is display-only and does not alter data」於 statCards。驗證：擴充 `Sustainability/configRender.test.ts` 斷言每個 `sustainability-stat-*` region 含 visible toggle 與 status select、seed 預設 normal/visible、runtime 套用 placeholder。

## 3. Seed fallback 與整體驗證

- [x] 3.1 五頁 seed config 對每張資料卡提供 `status` 預設（normal 或省略）與既有 `visible` 預設，確保舊 draft 缺欄位渲染不變。完成後：seed 重建後五頁資料卡預設正常顯示。驗證：擴充各頁 seed 測試（如 `packages/shared` 與頁面 `configRender` 測試）斷言預設值。
- [x] 3.2 跑全套前端測試與型別建置確認無回歸。完成後：卡片 status/visible 行為與 editor schema 全綠。驗證：`pnpm --filter @solar-display/web test` 與 `pnpm run build` 皆通過。
