## 1. Go collector prerequisite and red contract tests

- [x] 1.1 Confirm the user-provided active Go collector has landed, identify its package/service path, and run parity tests proving `solar/{SITE}/summary`, whole-zone data, status, heartbeat, and alert behavior matches the pre-hardening external contract; if the Go collector is absent or parity is not green, stop this change as blocked and do not modify the legacy Python collector as a substitute.
- [x] 1.2 Add red active-collector contract tests for `cmd/get-config`, `cmd/set`, retained sanitized `state/config`, non-retained `state/control-result`, safe-field allowlist, forbidden credential/topology fields, unknown-field atomic rejection, malformed envelopes, and unchanged Solar data topics; verify the new control cases fail before implementation.
- [x] 1.3 Add red tests for QoS duplicate/reconnect/restart idempotency, issuedAt/TTL expiry, future-clock skew bounds, request-result correlation, and restart replay; verify a repeated request id would cause a duplicate side effect before the idempotency implementation is added.

## 2. Harden the active collector control implementation

- [x] 2.1 Replace legacy `/config` request and `/set` command subscriptions in the active Go collector with `solar/{SITE}/cmd/get-config` and `cmd/set`, and publish `state/config`/`state/control-result` with the specified QoS/retain behavior; make command/state separation tests green and verify state publication cannot recursively invoke the command handler.
- [x] 2.2 Implement the positive sanitized config DTO and narrow remote-set registry, keeping credentials, broker topology, filesystem paths, arbitrary keys, and sensitive rejected values out of MQTT state/results/logs; verify seeded-secret tests cannot recover those values from retained config or control results.
- [x] 2.3 Implement atomic set validation/persistence and bounded safe result codes/messages, including successful changed-key reporting and rejected validation cases; verify an invalid multi-field request applies none of its requested changes.
- [x] 2.4 Implement a bounded persistent `(site, requestId)` processed-command ledger plus issuedAt/TTL validation, record completion before restart side effects, and return duplicate/prior results without reapplying commands; verify duplicate delivery across collector restart cannot trigger a second restart/config mutation.
- [x] 2.5 Disable execution of legacy `solar/{SITE}/set` and `/config` request semantics; if legacy traffic diagnostics are retained, verify they expose only bounded deprecation metadata and never execute the payload or republish full config.

## 3. Mosquitto authorization and protected control transport

- [x] 3.1 Simplify the active Go collector according to **Treat broker authorization as optional for the single-operator trusted LAN**, **Allow operator-selected plaintext LAN MQTT without mandatory TLS**, **Trusted single-operator LAN broker does not require role ACLs**, **MQTT authentication and TLS are optional deployment inputs**, and **Application command subscriptions remain bounded to configured factories**: missing `SOLAR_MQTT_USERNAME`/`SOLAR_MQTT_PASSWORD`/TLS inputs SHALL no longer block `run`, tray, `test-mqtt`, `start.sh`, or `start.ps1`, while explicitly supplied credentials/TLS remain usable and invalid explicit TLS still fails safely. Add/update focused tests proving unauthenticated localhost and `192.168.31.62` options validate, optional auth/TLS is preserved, configured CL/KN command subscriptions remain bounded, and no password enters config/state/log output.
- [x] 3.2 Configure control-capable clients to use loopback/local protected transport on the central server; if any control client is remote, configure TLS with certificate verification and verify plaintext LAN control authentication is not accepted as the production path. This covers **Prefer loopback for central control clients; require TLS if remote** and **Control credentials are not exposed over an untrusted plaintext network**.

> Scope note: task 3.2 records the previously completed strict transport witness. The user's later single-operator trusted-LAN decision supersedes it through revised task 3.1; it is preserved as historical completed work rather than rewritten.
- [x] 3.3 Explicitly deny or leave unsubscribed legacy privileged `/set`/`config` command paths after cutover and verify an ordinary legacy publisher cannot mutate the active collector even if it still publishes old topics.

## 4. Player/operator integration and retained-secret scrub

- [x] 4.1 Update only the Solar Player/operator diagnostics or control publisher code that actually consumes collector control state to use the new command/state/result contract; if Solar Player does not own control publishing, do not add an unnecessary management credential/client and verify Solar generation ingest remains unchanged.
- [x] 4.2 Perform the retained-state migration for every configured factory by clearing legacy retained `solar/{SITE}/config` messages with a narrowly targeted broker operation; this covers **Scrub old retained messages as a mandatory migration step**, **Legacy retained config secrets are actively removed during cutover**, and **Deployment cutover clears legacy retained config state**. Verify a fresh subscriber receives no legacy retained config while Solar summary/zone/new sanitized `state/config` retained messages remain intact.
- [x] 4.3 Update PC deployment/runbook material with external credential provisioning, ACL/transport setup, authorization tests, credential rotation, retained-message scrub, service reload order, troubleshooting, and rollback; verify no production passwords/private keys are added to Git and runbook tests/checks cover the commands or config templates maintained in the repo.

> Scope note: task 4.3 records the previously completed strict broker runbook. The user's later single-operator trusted-LAN decision supersedes its identity, ACL, TLS, credential-rotation, and service-reload procedure through revised task 5.4; only the retained scrub, optional hardening compatibility, troubleshooting, rollback, and secret-free documentation constraints remain current.

## 5. Security acceptance and verification

- [x] 5.1 Run black-box acceptance for valid get/set, forbidden credential mutation, unknown/malformed fields, expired/future commands, duplicate QoS delivery, atomic `restart=true` rejection with `RESTART_UNSUPPORTED`, authorized/unauthorized publishers, sanitized retained state, result non-retention, and legacy-topic non-execution; verify every case matches the capability scenarios **Collector commands and state use separate topic namespaces**, **Retained configuration state uses an explicit safe allowlist**, **Remote set commands use a narrow field allowlist**, **Every control command is correlated with a bounded non-retained result**, **Control commands are idempotent and reject stale replay**, **Legacy control topics do not remain an unprotected compatibility backdoor**, **Do not keep executable legacy command compatibility**, and **Existing Solar data and health topics keep their data semantics**. The acceptance checklist also covers the design decisions Use command/state topic separation with a bounded result channel, Define explicit sanitized state rather than serializing internal Config, and Restrict `cmd/set` to non-secret operational tuning.
- [x] 5.2 Re-run Go collector Solar data/status/heartbeat/alert parity tests and Solar Player adapter/aggregation diagnostics tests to prove control hardening did not change generation/zone data semantics.
- [x] 5.3 Run affected deployment/security tests, `git diff --check`, and `pnpm verify` where applicable; confirm no Python→Go port work, legacy Python fallback edits, MQTT credential commits, broad retained-message purge, or unrelated data-topic rename entered this change.
- [x] 5.4 Apply **Document trusted single-operator broker operation** and the revised Implementation Contract across deployment artifacts: remove the mandatory Mosquitto identity/ACL/TLS/credential-rotation/WinRM cutover procedure and its dedicated ACL template, document `192.168.31.62` plaintext/anonymous operation as the accepted trusted-LAN minimum, retain optional auth/TLS compatibility notes without making them gates, and revise the witness so the anonymous Windows broker is an accepted scope decision rather than an incomplete production cutover. Verify runbook/deployment tests, focused Go MQTT/start-script tests, secret scans, `git diff --check`, strict Spectra validation, and `pnpm verify`; confirm retained scrub, sanitized state, allowlist, TTL/idempotency, legacy-topic rejection, and `RESTART_UNSUPPORTED` remain unchanged.

## 6. Unsupported restart action reconciliation

- [x] 6.1 Implement and verify the Windows-only control contract for `restart=true`: return stable `RESTART_UNSUPPORTED`, atomically reject all accompanying changes, persist or return only the bounded rejection permitted by the existing ledger contract, keep the process running, and remove any supervisor/exit behavior; verify with focused control-contract tests covering fresh and duplicate request ids. This completes **Return safe structured control results**, **Persist command idempotency keys across reconnect/restart**, **Bound command age with issuedAt + ttlSeconds**, and the design decision **Gate apply on the user's Go collector landing**.
