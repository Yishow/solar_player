## 1. Red Tests And Contract Locks

- [x] [P] 1.1 Add failing dev-startup coverage for Listen ports are separate from runtime routing and Put dev proxy routing in Vite and dev startup topology, proving custom VITE_PORT remains only the web listen port, same PORT/VITE_PORT fails before launch, and portsToFree includes both resolved web and backend ports; verify with node --test scripts/dev.test.mjs.
- [x] [P] 1.2 Add failing web runtime-origin coverage for Dev browser runtime uses same-origin routes for runtime traffic and Use same-origin runtime paths in dev instead of client-side backend-port guessing, proving /api, /socket.io, and /uploads stay same-origin in dev mode with VITE_PORT=4173; verify with node --import tsx --test on the focused web service tests.
- [x] [P] 1.3 Add failing web override coverage for Explicit API base URL is reserved for deployment override mode and Keep explicit API base URL for production and preview only, proving VITE_API_BASE_URL produces one consistent API/socket/uploads origin while empty dev config stays same-origin; verify with node --import tsx --test on the focused web service tests.
- [x] [P] 1.4 Add failing Vite topology coverage for Vite dev server proxies runtime paths to the resolved backend port, proving /api, /socket.io with websocket support, and /uploads all target the resolved backend PORT; verify with a Vite config unit/source test that fails before proxy ownership is centralized.
- [x] [P] 1.5 Add or update failing server boundary coverage for Management boundaries remain protected under dev proxy topology and Preserve management access and socket session boundaries, proving management-only read routes still deny untrusted callers and Socket.IO sessions still classify playback-safe versus management-trusted; verify with targeted server tests for management auth and socket request/session behavior.

## 2. Runtime Origin And Proxy Implementation

- [x] 2.1 Implement the shared web runtime-origin helper so Dev browser runtime uses same-origin routes for runtime traffic across API, Socket.IO, and uploaded media, then wire apps/web/src/services/api.ts, apps/web/src/services/socket.ts, and apps/web/src/pages/shared/runtimeMediaUrl.ts through that helper; verify the tests from 1.2 pass without per-port exceptions for 4173.
- [x] 2.2 Implement Vite dev server proxies runtime paths to the resolved backend port and Put dev proxy routing in Vite and dev startup topology by routing /api, /socket.io, and /uploads through apps/web/vite.config.ts using the resolved backend PORT; verify the tests from 1.4 pass and websocket proxy support remains explicit.
- [x] 2.3 Implement Listen ports are separate from runtime routing in scripts/dev-lib.mjs and scripts/dev.mjs so PORT and VITE_PORT remain listen-port settings, startup rejects equal ports before launch, and conflict cleanup covers only the resolved web/backend ports; verify node --test scripts/dev.test.mjs passes.
- [x] 2.4 Implement Explicit API base URL is reserved for deployment override mode by keeping VITE_API_BASE_URL as the only explicit production/preview origin override, updating .env.example wording to describe that contract, and ensuring empty dev config never infers backend origin from VITE_PORT; verify the tests from 1.3 pass and content review confirms the env comments match the behavior.
- [x] 2.5 Preserve Management boundaries remain protected under dev proxy topology by keeping backend CORS, management read access, and Socket.IO session classification compatible with proxy traffic without broad same-host cross-port allowlist expansion; verify the tests from 1.5 pass.

## 3. Verification And Smoke

- [x] 3.1 Run the focused automated verification set for this change: node --test scripts/dev.test.mjs, the targeted web node --import tsx --test files from tasks 1.2 through 1.4, and the targeted server node --import tsx --test files from task 1.5; completion requires every command to pass or any environment-only blocker to be reported with the exact failing command and error.
- [x] 3.2 Run package build verification for the topology change with pnpm --filter @solar-display/server build and pnpm --filter @solar-display/web build; completion requires both builds to pass without TypeScript or Vite config errors.
- [x] 3.3 Run a manual or automated smoke with VITE_PORT=4173 proving /settings/playback, /settings/images, and /images can load API, Socket.IO, and /uploads traffic through the dev proxy without 404, CORS policy errors, or WebSocket origin failures; record the observed request origins and any acceptable cache status such as 304 separately from real 4xx/5xx failures.
