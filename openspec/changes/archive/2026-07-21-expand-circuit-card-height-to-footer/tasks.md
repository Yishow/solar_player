## 1. 調整卡片高度

- [x] 1.1 修改 `apps/web/src/pages/CircuitSettings/circuitSettings.css` 裡的 `.cs-page .cs-card` 高度，以實現 `Align circuit card height to settings page footer`。驗證：執行 `pnpm --filter @solar-display/web build` 成功。
