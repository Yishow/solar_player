## 1. Boundary 文件與 closeout contract

- [x] [P] 1.1 建立 `docs/reference-match/fhd-reference-informed-closeout-boundaries.md`，交付 `Classify Reference Differences Before Tuning` 決策與 `Classify material reference differences before FHD closeout implementation`、`Protected product choices require bounded evidence`、`Reference quality targets describe direction without pixel matching`、`Actual gaps remain actionable and verifiable` 的分類 contract；完成後以內容審查與 `rg "protected-product-choice|reference-quality-target|actual-gap|Accepted By|Revisit Trigger" docs/reference-match/fhd-reference-informed-closeout-boundaries.md` 驗證三個 token、必要欄位、header/footer protected shell 範例與五頁 page-content target 範例都存在。
- [x] [P] 1.2 更新 `docs/reference-match/fhd-evidence-bundle-template.md`，交付 `Use Evidence Bundle As Boundary Ledger` 決策與 `Record reference-informed boundary decisions in FHD evidence bundles`、`Treat incomplete boundary evidence as incomplete FHD evidence` 的可填寫模板；完成後以內容審查與 `rg "Boundary decision|Current Product Choice|Protected Attributes|Implementation Consequence|Accepted By|Revisit Trigger" docs/reference-match/fhd-evidence-bundle-template.md` 驗證 evidence bundle 有固定 boundary decision 欄位，且缺 owner 或 protected attributes 不能豁免 visual review 的規則已寫入。
- [x] [P] 1.3 更新 `docs/display-surface-visual-review-checklist.md`，交付 `Treat Header And Footer As Protected Shell Boundaries When Accepted` 決策與 `Review intentional reference differences as scoped boundary decisions`、`Preserve reference-inspired visual language without management-surface drift`、`Boundary vocabulary is testable through lightweight assertions` 的 review contract；完成後以內容審查與 `rg "scoped boundary|management-surface drift|protected-product-choice|reference-quality-target|actual-gap" docs/display-surface-visual-review-checklist.md` 驗證 checklist 能保護 accepted shell 且仍拒絕 playback drift。
- [x] [P] 1.4 更新 `docs/reference-match/display-launch-witness-matrix.md` 與 `docs/fhd-witness/playback-closeout-matrix.md`，交付 `Keep Launch Matrix As Status Ledger` 決策與 `Boundary decisions remain supporting evidence rather than launch status ledgers`、`Distinguish protected choices from unresolved gaps in launch witness status`、`Reference-informed boundary decisions are linked from the authoritative witness matrix`、`Five-page closeout uses consistent reference-informed examples` 的 status contract；完成後以內容審查與 `rg "boundary|protected-product-choice|reference-quality-target|actual-gap" docs/reference-match/display-launch-witness-matrix.md docs/fhd-witness/playback-closeout-matrix.md` 驗證 matrix 引用 boundary rationale，且五頁既有 pass/fail/blocked 狀態沒有被這個 artifact-only change 改成 launch-ready。
- [x] [P] 1.5 更新 `docs/fhd-editor-gap-ledger.md`，交付 closeout handoff contract：accepted header/footer shell choices 不被列成 editor capability gap，page content 的 hero、KPI、flow、circuit、media stage、caption、ornament、highlight rail 差異仍分類為 `reference-quality-target` 或 `actual-gap`；完成後以內容審查與 `rg "protected-product-choice|reference-quality-target|actual-gap|header|footer" docs/fhd-editor-gap-ledger.md` 驗證 ledger 能區分 accepted shell choice 與仍需處理的 page content gap。

## 2. Focused tests

- [x] [P] 2.1 更新 `apps/web/src/pages/fhdEvidenceWorkflow.test.ts`，交付 evidence workflow 的文件 contract assertion：測試能檢查 boundary decision table 欄位、三種 classification token、以及 incomplete protected choice 不得豁免 review 的語彙；完成後以 `pnpm --filter @solar-display/web test -- src/pages/fhdEvidenceWorkflow.test.ts` 驗證。
- [x] [P] 2.2 更新 `apps/web/src/pages/displaySurfaceVisualGuardrails.test.ts`，交付 `Guard The Vocabulary With Focused Tests` 決策：測試能檢查 visual checklist 包含 scoped boundary decision、header/footer protected shell 範例、management-surface drift 防線與三種 classification token；完成後以 `pnpm --filter @solar-display/web test -- src/pages/displaySurfaceVisualGuardrails.test.ts` 驗證。
- [x] [P] 2.3 更新 `apps/web/src/pages/displayLaunchWitnessGates.test.ts`，交付 launch matrix 文件 contract assertion：測試能檢查 authoritative matrix 仍保留 pass/fail/blocked ledger，且 boundary classification 只解釋 status rationale、不取代五頁 launch status；完成後以 `pnpm --filter @solar-display/web test -- src/pages/displayLaunchWitnessGates.test.ts` 驗證。

## 3. Validation 與交接

- [x] 3.1 跑 focused web test pack，確認三個文件 contract tests 同時通過；完成後以 `pnpm --filter @solar-display/web test -- src/pages/fhdEvidenceWorkflow.test.ts src/pages/displaySurfaceVisualGuardrails.test.ts src/pages/displayLaunchWitnessGates.test.ts` 驗證。
- [x] 3.2 跑 Spectra artifact gate，確認 proposal、design、specs、tasks 與實作後文件一致；完成後以 `spectra analyze define-fhd-reference-informed-closeout-boundaries --json` 無 Critical/Warning 且 `spectra validate define-fhd-reference-informed-closeout-boundaries --strict` 通過驗證。
- [x] 3.3 依 repo 規則更新 graphify knowledge graph，確認實作後的 docs/tests 關係可被查詢；完成後以 `graphify update .` 完成或明確記錄受限 sandbox fallback 結果驗證。
