## Tasks

- [x] Implement `Model display-page media effects as typed composable layers` by `Model media effects as typed layers instead of named booleans` in shared types and normalized resolver outputs.
- [x] Implement `Normalize zone vocabulary and support typing in shared code` by `Normalize zones and support typing in shared code` for effect kinds, zone vocabulary, and per-surface support matrices.
- [x] Implement `Keep legacy effect data readable while canonical writing moves forward` by `Provide legacy compatibility before new authoring becomes canonical` for existing blur/fade/opacity seed and draft data.
- [x] Add shared tests for canonical layer normalization, clamping, stacking shape, and legacy compatibility.
- [x] Run affected shared and web tests for media effect schema consumers.
