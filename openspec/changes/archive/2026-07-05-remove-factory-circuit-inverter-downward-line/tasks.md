## 1. UI 調整

- [x] 1.1 移除 `apps/web/src/pages/FactoryCircuit/index.tsx` 中的逆變器垂直下墜向量連線 SVG 區塊，以移除逆變器下方的垂直線與流動粒子。確認在 `/factory-circuit` 頁面上，逆變器下方無任何線條。
- [x] 1.2 執行 `pnpm run fhd:witness` 生成 /factory-circuit 的 1920x1080 螢幕截圖與 evidence，並填寫 `docs/fhd-witness/evidence-template.md` 進行手動內容審查驗證。
