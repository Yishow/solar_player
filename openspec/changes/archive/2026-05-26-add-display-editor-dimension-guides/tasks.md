## 1. Overlay State Contract

- [x] 1.1 Implement **Map `DisplayEditorCanvasCard` viewport space to configurable design space** so `useDisplayEditorCanvasWorkflow` and related canvas math convert pointer, guide, and measurement coordinates between the preview viewport and the default `1920x1080` design space for **Render page-aligned guide overlays in the editor viewport with design-space mapping**, and verify with focused mapping assertions in `apps/web/src/pages/DisplayPagesEditor/canvasInteractions.test.ts` or a dedicated workflow test plus `pnpm --filter @solar-display/web test`.
- [x] 1.2 Implement **Extend the canvas workflow with explicit overlay feedback state** so `DisplayEditorCanvasOverlay` receives active guide, rect, constraint, design-space, and overlay-mode data that keeps **Show live dimensions for the selected canvas region** and **Render page-aligned guide overlays in the editor viewport with design-space mapping** aligned with zoom/pan behavior, and verify with markup/render assertions in `apps/web/src/pages/DisplayPagesEditor/inspectorFields.test.tsx`, route rendering coverage in `apps/web/src/pages/DisplayPagesEditor/index.test.tsx`, and one manual zoom/pan check at `/display-pages/editor?page=overview`.
- [x] 1.3 Implement **Persist overlay presets and expose display-density controls** so the canvas controls and overlay state satisfy **Persist and restore overlay guide presets**, and verify with preset restore assertions in `apps/web/src/pages/DisplayPagesEditor/index.test.tsx` plus `pnpm --filter @solar-display/web test`.

## 2. Guide, Mode, And Measurement Behavior

- [x] 2.1 Implement **Expose overlay display modes in `DisplayEditorCanvasCard` controls** so the controls area above the preview can switch **Expose overlay display modes for selected-only and full-canvas framing** between selected-only and full-canvas behavior without mutating any saved region geometry, and verify with UI coverage in `apps/web/src/pages/DisplayPagesEditor/index.test.tsx` and control rendering assertions around `apps/web/src/pages/DisplayPagesEditor/canvasCard.tsx`.
- [x] 2.2 Implement **Default region framing to selected-only with an optional full-frame mode** so the editor no longer frames every region at full strength after one selection and instead fulfills **Expose overlay display modes for selected-only and full-canvas framing**, and verify with selected-tile and full-frame mode cases in `apps/web/src/pages/DisplayPagesEditor/inspectorFields.test.tsx` or `apps/web/src/pages/DisplayPagesEditor/index.test.tsx` plus `pnpm --filter @solar-display/web test`.
- [x] 2.3 Implement **Separate persistent page guides from transient interaction measurements** and **Limit measurement scope to known geometry and container bounds** so full-page dashed guides, selected-region dimensions, and rail-card parent-bound measurements satisfy **Render page-aligned guide overlays in the editor viewport with design-space mapping** and **Show live dimensions for the selected canvas region**, and verify with parent-boundary cases in `apps/web/src/pages/DisplayPagesEditor/canvasInteractions.test.ts`, locked-region coverage in `apps/web/src/pages/DisplayPagesEditor/index.test.tsx`, and a content review of overlay states in the affected route.
- [x] 2.4 Implement guide axes, region labels, center lines, and non-selected frame density controls so the overlay satisfies **Persist and restore overlay guide presets** in both selected-only and full-canvas modes, and verify with new render assertions in `apps/web/src/pages/DisplayPagesEditor/inspectorFields.test.tsx` and `apps/web/src/pages/DisplayPagesEditor/index.test.tsx`.

## 3. Regression Verification

- [x] 3.1 Add regression coverage proving **Render page-aligned guide overlays in the editor viewport with design-space mapping** still works across page switches, design-size mapping, zoom/pan, selected-only mode, full-canvas mode, and missing-geometry fallbacks, and verify with updated `apps/web/src/pages/DisplayPagesEditor/index.test.tsx`, `apps/web/src/pages/DisplayPagesEditor/inspectorFields.test.tsx`, and a manual route check on one non-Overview page.
- [x] 3.2 Add regression coverage proving **Persist and restore overlay guide presets** survives reload and invalid stored state while keeping the editor usable, and verify with new web test cases plus a manual reload check on `/display-pages/editor`.
- [x] 3.3 Run `pnpm --filter @solar-display/web test`, `spectra analyze add-display-editor-dimension-guides --json`, and `spectra validate add-display-editor-dimension-guides` so the change remains analyzable and valid after implementation, and verify all three commands exit successfully.
