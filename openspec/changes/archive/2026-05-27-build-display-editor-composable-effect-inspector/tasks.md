## Tasks

- [x] Implement `Represent composable media effects as an editable layer list in Properties` by `Represent effects in the inspector as a layer list` with add, remove, enable, disable, reorder, and per-layer field editing.
- [x] Implement `Route visible canvas selections to the owning media effect source` by `Route visible container selections to the owning effect source` for canvas-first authoring.
- [x] Implement `Keep Source Connection effect information summary-only` by `Keep Source Connection summary-only` so editable effect controls stay inside `屬性`.
- [x] Add editor tests for layer lifecycle, per-layer fields, unsupported surfaces, and selection routing.
- [x] Run affected web tests for DisplayPagesEditor composable effect authoring.
