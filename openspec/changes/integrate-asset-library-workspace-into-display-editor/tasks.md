## Tasks

- [ ] Implement `Expose asset library management inside Display Pages Editor` by adding an Asset Library workspace entry inside `/display-pages/editor`.
- [ ] Apply the `Workspace Model` design so the gallery renders inside the editor workspace layout and preserves editor return context.
- [ ] Implement `Provide editor-grade gallery controls and usage visibility` with category filtering, search, thumbnail density, upload entry, asset detail preview, reference counts, usage locations, and delete guard states.
- [ ] Apply `Gallery Depth` by making asset cards, preview/detail, upload, usage, and blocked delete states visible in the integrated workspace.
- [ ] Wire page and shell asset pickers to open or deep-link into the integrated gallery without losing edit context.
- [ ] Apply `Legacy Route Handling` by keeping `/settings/assets` as redirect or secondary compatibility entry to the editor workspace.
- [ ] Add route and component tests for the integrated asset workspace.
- [ ] Run affected web tests for DisplayPagesEditor, AssetLibrary, and routing.
