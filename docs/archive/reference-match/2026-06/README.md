# 2026-06 Historical FHD Evidence

這個資料夾集中保存 2026-06 的 FHD closeout 證據、review、handoff 與 page-specific refinement 記錄。

這些文件仍保留在 repo 內，原因如下：

- `openspec/` 仍需要它們作為 trace 與 supporting evidence。
- 它們對理解 2026-06 的 FHD closeout 決策與視覺差距仍有參考價值。
- 它們不是現行 workflow 入口，也不是 launch status 的 source of truth。

現行入口請優先改讀：

- `docs/reference-match/fhd-workflow-entrypoints.md`
- `docs/reference-match/playback-visual-canonicals.md`
- `docs/reference-match/fhd-reference-informed-closeout-boundaries.md`
- `docs/reference-match/display-launch-witness-matrix.md`
- `docs/reference-match/display-launch-verification-pack.md`

若未來要再把這一批檔案移出 `reference-match/`，必須先同步更新 `openspec/`、相關 review docs 與任何仍保留的 trace 引用。
